

services:
  web:
    build:
      context: ./nginx_aux
    container_name: web_server
    ports:
      - "8080:80"
    volumes:
      - ./site:/usr/share/nginx/html
    networks:
      - app_net

  waf:
    build:
      context: ./WAF
    container_name: modsecurity_proxy
    ports:
      - "8443:443"
    depends_on:
      - web
    networks:
      - app_net

networks:
  app_net:
    driver: bridge
